% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdrm_grouped_methods_new.R
\name{hdrm_grouped}
\alias{hdrm_grouped}
\title{Multiple-group inference for high-dimensional repeated measures}
\usage{
hdrm_grouped(
  data,
  hypothesis = "whole",
  AM = TRUE,
  group,
  subject = NULL,
  cov.equal = FALSE,
  subsampling = FALSE,
  B = "1000*N",
  seed = NULL
)
}
\arguments{
\item{data}{A numeric vector or matrix. For matrix input, subjects are
represented by rows and repeated-measurement dimensions by columns. For
vector input, \code{subject} and \code{group} must identify the subject and group
associated with every measurement.}

\item{hypothesis}{Either one of \code{"whole"}, \code{"sub"}, \code{"interaction"},
\code{"identical"}, or \code{"flat"}, or a named list containing the projection
matrices \code{TW} and \code{TS}; see Details.}

\item{AM}{A single logical value, or alternatively \code{0} or \code{1}, specifying
whether the compact representation of the hypothesis matrices described by
\insertCite{Sattler2025;textual}{hdrm} is used. It may reduce the number of
rows used in the calculations without changing the resulting test. The
default is \code{TRUE}.}

\item{group}{A one-dimensional atomic vector or factor defining the group
allocation. For matrix input it must contain one entry per row. For
vector input it must contain one entry per measurement.}

\item{subject}{An optional one-dimensional atomic vector identifying
subjects. It is required for vector input and ignored with a warning for
matrix input. Subject labels need only be unique within groups and may be
reused in different groups.}

\item{cov.equal}{A single logical value specifying whether the group
covariance matrices are assumed to be equal. The default is \code{FALSE}.}

\item{subsampling}{A single logical value specifying whether the
subsampling versions of all available trace estimators are used in the
heterogeneous-covariance procedure. It has no effect when
\code{cov.equal = TRUE}; see Details.}

\item{B}{A single numeric value or arithmetic character expression in \code{N}
defining the subsampling budget. Character expressions may contain only
numeric constants, \code{N}, parentheses, and the operators \code{+}, \code{-}, \code{*}, \code{/},
and \code{^}. Its interpretation depends on \code{cov.equal} and \code{subsampling}; see
Details.}

\item{seed}{\code{NULL} or a single integer-valued number used to make stochastic
calculations reproducible. When supplied, the seed is applied locally and
the previous R random-number state is restored after the calculation.}
}
\value{
A named list of class \code{"hdrm_grouped"} with components:
\describe{
\item{data}{The processed data matrix with subjects in rows,
repeated-measurement dimensions in columns, and subjects ordered by group.}
\item{statistic}{The standardized test statistic \eqn{W}.}
\item{f}{The estimated degrees of freedom.}
\item{tau}{The estimated convergence parameter \eqn{\tau=1/f}.}
\item{H}{A named list containing the projection matrices \code{TW} and \code{TS}.}
\item{hypothesis}{The selected predefined hypothesis or \code{"custom"}.}
\item{p.value}{The upper-tail p-value, bounded below by
\code{.Machine$double.eps}.}
\item{dim}{A named list containing the repeated-measurement dimension \code{d}
and the number of analyzed subjects \code{N}.}
\item{groups}{A named list containing the number of analyzed groups \code{a}
and their subject counts in \code{table}.}
\item{removed.cases}{The number of incomplete subjects removed before the
analysis.}
\item{subsamples}{The evaluated integer base budget \code{B}. The grouped
third-trace estimators use \eqn{aB} draws; see Details.}
}
}
\description{
Implements the multiple-group procedure allowing heterogeneous covariance
matrices of \insertCite{Sattler2018;textual}{hdrm} and the multiple-group
procedure under equal covariance matrices of
\insertCite{Sattler2021;textual}{hdrm}.
}
\details{
For vector input, missing values in \code{data} cause the entire affected subject
to be removed. The vectors \code{subject} and \code{group} must not contain missing
values. For matrix input, every row containing at least one missing value
is removed. A warning is issued whenever incomplete subjects are dropped.

For vector input, repeated measurements must occur in the same order for
every subject. No separate variable identifying the repeated-measurement
dimension is supplied, so the within-subject order in \code{data} determines the
component order in the processed data matrix. The observations themselves
need not be globally sorted by subject or group.

At least two groups and two repeated-measurement dimensions are required.
Every group must contain at least six complete subjects.

The tested hypothesis has the form
\deqn{(\bm T_W \otimes \bm T_S)\bm\mu=\bm 0.}
The predefined hypotheses are:
\itemize{
\item \code{"whole"}:
\eqn{\bm T_W=\bm P_a} and
\eqn{\bm T_S=\bm J_d/d}; no whole-plot or group main effect.
\item \code{"sub"}:
\eqn{\bm T_W=\bm J_a/a} and
\eqn{\bm T_S=\bm P_d}; no subplot or dimension main effect.
\item \code{"interaction"}:
\eqn{\bm T_W=\bm P_a} and
\eqn{\bm T_S=\bm P_d}; no group-by-dimension interaction.
\item \code{"identical"}:
\eqn{\bm T_W=\bm P_a} and
\eqn{\bm T_S=\bm I_d}; identical expectation vectors across groups.
\item \code{"flat"}:
\eqn{\bm T_W=\bm I_a} and
\eqn{\bm T_S=\bm P_d}; a flat expectation profile in every group.
}

Alternatively, \code{hypothesis} may be a named list containing \code{TW} and \code{TS}.
Both matrices must be finite, symmetric, idempotent projection matrices
with positive rank. \code{TW} must have one row and column per analyzed group,
and \code{TS} must have one row and column per repeated-measurement dimension.
Small numerical deviations within the implemented tolerance are accepted.

When \code{cov.equal = FALSE}, the method of
\insertCite{Sattler2018;textual}{hdrm} is used. The third-trace estimator
entering \code{f} is always computed by subsampling. Here, \code{B} is the base
subsampling budget. Group-specific and pairwise subsampling estimators use
\code{B} draws for each group or group pair, respectively. The joint third-trace
estimator uses \eqn{aB} joint draws, where each draw simultaneously samples
six subjects from every group. When \code{subsampling = TRUE}, the remaining
available trace estimators are also replaced by their subsampling versions.

When \code{cov.equal = TRUE}, the method of
\insertCite{Sattler2021;textual}{hdrm} is used and \code{subsampling} has no
effect. The pooled third-trace estimator uses an exact total of \eqn{aB}
six-subject draws across all groups. These draws are allocated
approximately proportionally to \eqn{\binom{n_i}{6}} using the
largest-remainder method, with every group receiving at least one draw.

Even when \code{subsampling = FALSE}, \code{f}, \code{tau}, and \code{p.value} remain seed
dependent because a third-trace quantity is estimated by subsampling. For
heterogeneous covariance matrices with \code{subsampling = TRUE}, \code{statistic}
is seed dependent as well.

\code{B} may be numeric or an arithmetic character expression involving \code{N},
such as \code{"1000*N"} or \code{"10*(N + 1)"}. The result is rounded up to the next
integer. Functions, assignments, indexing, and additional variable names
are rejected and are never evaluated.

Upper-tail probabilities are computed directly. Reported p-values are
bounded below by \code{.Machine$double.eps}; a returned value at this boundary
should be interpreted as no larger than the numerical reporting threshold.
}
\examples{
# Long-format data ---------------------------------------------------------

data("EEG")

# Subject labels need only be unique within groups. The original labels can
# therefore be used directly, even when the same label occurs in another group.
eeg_grouped <- EEG[
  order(EEG$group, EEG$subject, EEG$dimension),
]

# Heterogeneous covariance matrices; the available exact trace estimators are
# used, while the third-trace quantity is still estimated by subsampling.
hdrm_grouped(
  data = eeg_grouped$value,
  hypothesis = "whole",
  group = eeg_grouped$group,
  subject = eeg_grouped$subject,
  cov.equal = FALSE,
  subsampling = FALSE,
  B = "100*N",
  seed = 3141
)

# Replace the remaining available trace estimators by their subsampling
# versions. Here B is used by each subsampling-estimator invocation.
hdrm_grouped(
  data = eeg_grouped$value,
  hypothesis = "sub",
  group = eeg_grouped$group,
  subject = eeg_grouped$subject,
  cov.equal = FALSE,
  subsampling = TRUE,
  B = 10000,
  seed = 3141
)

# A custom projection-matrix hypothesis equivalent to hypothesis = "sub".
custom_eeg_hypothesis <- list(
  TW = matrix(1 / 4, nrow = 4, ncol = 4),
  TS = diag(40) - matrix(1 / 40, nrow = 40, ncol = 40)
)

hdrm_grouped(
  data = eeg_grouped$value,
  hypothesis = custom_eeg_hypothesis,
  group = eeg_grouped$group,
  subject = eeg_grouped$subject,
  B = "100*N",
  seed = 3141
)


# Wide matrix data ---------------------------------------------------------

data("birthrates")

birthrates_matrix <- t(as.matrix(birthrates))

group <- factor(
  c(1, 1, 2, 2, 1, 1, 1, 2, 1, 1, 1, 1, 2, 2, 1, 2),
  labels = c("west", "east")
)

hdrm_grouped(
  data = birthrates_matrix,
  hypothesis = "interaction",
  group = group,
  cov.equal = FALSE,
  subsampling = FALSE,
  B = "100*N",
  seed = 3141
)

# Under equal covariance matrices, B is the total pooled subsampling budget
# across groups and the value of subsampling is ignored.
hdrm_grouped(
  data = birthrates_matrix,
  hypothesis = "whole",
  group = group,
  cov.equal = TRUE,
  B = "100*N",
  seed = 3141
)

d <- ncol(birthrates_matrix)
custom_birthrates_hypothesis <- list(
  TW = matrix(1 / 2, nrow = 2, ncol = 2),
  TS = diag(d) - matrix(1 / d, nrow = d, ncol = d)
)

hdrm_grouped(
  data = birthrates_matrix,
  hypothesis = custom_birthrates_hypothesis,
  group = group,
  B = "100*N",
  seed = 3141
)
}
\references{
\insertAllCited
}
